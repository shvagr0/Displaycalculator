<Window x:Class="MonitorCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="ThreeDBorderWindow"
        Background="White"
        Icon="\icon.ico"
        Title="DisplayCalculator" Height="350" Width="500">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="WarningBox" TargetType="TextBox">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Background" Value="#FA66"/>
            </Style>
            <Style x:Key="CloseBox" TargetType="TextBox">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Background" Value="#FFE6E678"/>
            </Style>
            <Style x:Key="OpenBox" TargetType="TextBox">
                <Setter Property="IsEnabled" Value="True"/>
                <Setter Property="Background" Value="White"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Content="Diagonal" Grid.Row="0"/>
            <TextBox Text="112" Grid.Row="1"/>
            <ComboBox Grid.Column="1" Grid.RowSpan="2" Margin="20,30,20,20" FontSize="8">
                <ComboBoxItem IsSelected="True">d</ComboBoxItem>
                <ComboBoxItem>cm</ComboBoxItem>
                <ComboBoxItem>mm</ComboBoxItem>
            </ComboBox>

        </Grid>


    </Grid>
</Window>
